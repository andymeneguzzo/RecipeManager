<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- PERMESSI -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RecipeManager"
        tools:targetApi="33">

        <receiver android:name=".NotificationReceiver" />

        <!-- LAUNCHER -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- OTHER ACTIVITIES -->
        <activity android:name=".activities.GreetingsActivity"/>
        <activity android:name=".activities.AddRecipeActivity"/>
        <activity android:name=".activities.EditRecipeActivity"/>
        <activity android:name=".activities.UserActivity"/>
        <activity android:name=".activities.AddStepsActivity"/>
        <activity android:name=".activities.EditStepsActivity"/>
        <activity android:name=".activities.SettingsActivity"/>
        <activity android:name=".activities.IconsListActivity"/>
        <activity android:name=".activities.ViewStepsActivity"/>
        <activity android:name=".activities.ViewInfoActivity"/>

        <!-- DRAWER LIST ACTIVITY -->
        <activity android:name=".drawer_activities.BurgerListActivity"/>
        <activity android:name=".drawer_activities.DessertListActivity"/>
        <activity android:name=".drawer_activities.FishListActivity"/>
        <activity android:name=".drawer_activities.MeatListActivity"/>
        <activity android:name=".drawer_activities.PastaListActivity"/>
        <activity android:name=".drawer_activities.PizzaListActivity"/>
        <activity android:name=".drawer_activities.SushiListActivity"/>
        <activity android:name=".drawer_activities.VeggieListActivity"/>


        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>