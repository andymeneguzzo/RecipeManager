<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.RecipeManager" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colori primari -->
        <item name="colorPrimary">@color/top_bottom_bar_background</item>
        <item name="colorPrimaryContainer">@color/recipies</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Colori secondari -->
        <item name="colorSecondary">@color/top_bottom_bar_background</item>
        <item name="colorSecondaryContainer">@color/recipies</item>
        <item name="colorOnSecondary">@color/white</item>

        <!-- Background e surface: usiamo il nero per la modalità scura -->
        <item name="android:colorBackground">@color/black</item>
        <item name="colorSurface">@color/black</item>

        <!-- Colori dei testi: testi chiari su sfondo scuro -->
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
        <item name="android:editTextColor">@color/edit_text</item>
    </style>
</resources>